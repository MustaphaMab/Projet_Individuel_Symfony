{% extends 'base.html.twig' %}

{% block title %}PaiementCommande{% endblock %}

{% block body %}
    <h1>PaiementCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement_commande.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement_commande.Montant }}</td>
            </tr>
            <tr>
                <th>Methode</th>
                <td>{{ paiement_commande.Methode }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ paiement_commande.Statut }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ paiement_commande.Date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paiement_commande_index') }}">back to list</a>

    <a href="{{ path('app_paiement_commande_edit', {'id': paiement_commande.id}) }}">edit</a>

    {{ include('paiement_commande/_delete_form.html.twig') }}
{% endblock %}
