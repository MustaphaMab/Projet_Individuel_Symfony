{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}
{# templates/cart/show.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<h1>Panier</h1>
<table>
    <tr>
        <th>Produit</th>
        <th>Quantité</th>
        <th>Action</th>
    </tr>
    {% for item in products %}
    <tr>
        <td>{{ item.product.nom }}</td>
        <td>{{ item.quantity }}</td>
        <td>
            <a href="{{ path('cart_add', {'id': item.product.id}) }}">Ajouter</a>
        </td>
    </tr>
    {% endfor %}
</table>
{% endblock %}

{% endblock %}
